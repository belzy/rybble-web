import React from 'react';
import {
  ButtonGroup,
} from '@chakra-ui/react';

const SocialButtonGroup = (): React.ReactElement => {

  return (
    <ButtonGroup spacing={ 4 }>

    </ButtonGroup>
  );
};

export default SocialButtonGroup;